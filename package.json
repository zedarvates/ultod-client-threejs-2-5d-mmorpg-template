{
  "name": "ultod-client-threejs-2-5d-mmorpg-template",
  "version": "0.1.3",
  "description": "Minimal isolated presentation shell for Three.js 2.5D isometric MMORPG client.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && npm run check:bundle",
    "check:bundle": "node scripts/check-build-chunks.mjs",
    "check:packages": "node scripts/check-package-consumers.mjs",
    "preview": "vite preview",
   "test:e2e": "playwright test",
    "check:public-boundary": "node scripts/check-public-boundary.mjs",
    "check:content-sdk": "npm --workspace @ultod/content-sdk run typecheck",
    "check:client-core": "npm --workspace @ultod/content-sdk run build && npm --workspace @ultod/threejs-client-core run typecheck"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/three": "^0.180.0",
    "@types/ws": "^8.18.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.0",
    "ws": "^8.21.3"
  },
  "license": "MIT"
}
